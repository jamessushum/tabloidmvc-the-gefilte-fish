@model TabloidMVC.Models.Tag

@{
    ViewData["Title"] = "Delete";
}


<div class="container pt-5">
    <div class="row justify-content-center">
        <div class="card col-md-8">
            <h4 class="mt-3 text-primary text-center card-title">Are you sure you want to delete this tag?</h4>
            <div class="mt-3 card-body">
                <form class="" asp-action="Delete">
                    <dl class="align-content-center">
                        <dt class="col-4 text-center">
                            @Html.DisplayNameFor(model => model.Name)
                        </dt>
                        <dd class="col-4 text-center">
                            @Html.DisplayFor(model => model.Name)
                        </dd>
                    </dl>
                    <input type="submit" value="Delete" class="btn btn-danger" />
                </form>
            </div>
        </div>
    </div>
    <div>
        <a asp-action="Index">Back to List</a>
    </div>
</div>
